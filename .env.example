# ============================================
# SERVER (.env in project root — used by Render)
# ============================================

# Server port (Render sets this automatically)
PORT=3001

# Bind address
HOST=0.0.0.0

# Must be "production" for deployment
NODE_ENV=production

# IMPORTANT: Generate a strong random secret!
# Run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=change_me_to_a_random_64_char_string

# Allowed frontend origins (comma-separated)
# Set to your Vercel URL + custom domain
CORS_ORIGINS=https://your-app.vercel.app,https://yourdomain.com

# Optional: Custom TURN server for video/audio calls
# TURN_URL=turn:your-turn-server.com:3478
# TURN_USERNAME=your_username
# TURN_CREDENTIAL=your_password


# ============================================
# CLIENT (set in Vercel dashboard → Environment Variables)
# ============================================

# Backend URL (your Render deployment URL, NO trailing slash)
# VITE_API_URL=https://your-app.onrender.com
